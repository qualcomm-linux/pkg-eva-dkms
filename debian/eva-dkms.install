# install all upstream sources; debian/rules excludes dotfiles and the debian dir
* usr/src/eva-driver-${env:DEB_VERSION_UPSTREAM}
debian/dkms-build usr/src/eva-driver-${env:DEB_VERSION_UPSTREAM}
debian/dkms-clean usr/src/eva-driver-${env:DEB_VERSION_UPSTREAM}
